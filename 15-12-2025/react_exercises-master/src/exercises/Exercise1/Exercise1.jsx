import React from 'react';

/**
 * ESERCIZIO 1: Props e Componenti Base
 * 
 * Obiettivo: Creare un componente UserCard che riceve nome, cognome ed età
 * come props e li visualizza in una card formattata. 
 * 
 * TODO:
 * 1. Definire i parametri della funzione per ricevere le props
 * 2. Creare la struttura JSX con:
 *    - Un div container per la card
 *    - Un h2 per nome e cognome
 *    - Un p per l'età
 * 3. Aggiungere uno stile inline di base alla card
 */

function UserCard(/* aggiungi parametri qui */) {
  // stile

  return (
    <div> 
      {/* Completa qui la struttura della card usando props */}
    </div>
  );
}

export default UserCard;