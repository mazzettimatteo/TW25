import React from 'react';

/**
 * ESERCIZIO 3: Liste e Map
 * 
 * Obiettivo: Renderizzare una lista di nomi usando map()
 * 
 * TODO:
 * 1. Creare un array di nomi
 * 2. Usare map() per trasformare l'array in elementi <li>
 * 3. Aggiungere l'attributo key a ogni elemento
 */

function NameList() {
  // TODO: definisci l'array di nomi
  // const names = ['Mario', 'Luigi', 'Peach', 'Toad'];

  return (
    <div style={{ padding: '20px', maxWidth: '400px' }}>
      <h2>Lista Nomi</h2>
      <ul>
        {/* TODO: usa map() per renderizzare i nomi */}
      </ul>
    </div>
  );
}

export default NameList;