import React, { useState } from 'react';

/**
 * ESERCIZIO 2: State con useState
 * 
 * Obiettivo: Creare un componente ToggleButton che cambia stato
 * da ON a OFF al click, modificando anche il colore di sfondo.
 * 
 * TODO:
 * 1. Creare una variabile di stato booleana (es. isOn) con useState
 * 2. Creare una funzione che inverte lo stato
 * 3. Creare un pulsante che:
 *    - Mostra "ON" o "OFF" in base allo stato
 *    - Ha sfondo verde quando ON, rosso quando OFF
 *    - Chiama la funzione di toggle al click
 */

function ToggleButton() {
  // TODO: definire la const isOn con useState qui

  // TODO: crea la funzione di toggle usando la funzione di useState

  // TODO: crea l'oggetto style condizionale 

  return (
    <div>
      {/* TODO: aggiungi il pulsante qui usando un operatore ternario "if x is true, then y, otherwise z" (x ? y : z) */}
    </div>
  );
}

export default ToggleButton;